<!DOCTYPE html>
<html>
<head>
  <title>Blocipedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container-fluid">
    <nav id="primary-navigation" class="row">
        <div class="col-md-12 tabbable">
            <ul class="nav nav-tabs" style="border-bottom:none;">
                <li class="active left links"><%= link_to "Blocipedia", root_path, :class => 'navbar-link' %></li>
                <li class="left links"><%= link_to "About", about_path, :class => 'navbar-link' %></li>
                <li class="left links"><%= link_to "Wikis", wikis_path %></li>

              <div class="right nav nav-tabs"  style="border-bottom:none;">
                <% if user_signed_in? || current_user %>
                  User: <strong><%= current_user.email %></strong> |
                  <%= link_to 'Edit Profile', edit_user_registration_path, :class => 'navbar-link' %> or <%= link_to "Sign Out", destroy_user_session_path, :class => 'navbar-link'  %>

                  <% else %>
                <span class="sign-up-in-links">
                  <li><%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %></li>
                </span>
              <% end %>
            </div>
            </ul>
        </div>
    </nav>

    <% if flash[:notice] %>
      <div class="alert alert-success">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <%= flash[:notice] %>
      </div>
    <% elsif flash[:alert] %>
      <div class="alert alert-warning">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <%= flash[:alert] %>
      </div>
    <% end %>

      <%= yield %>
  </div>
</body>
</html>
