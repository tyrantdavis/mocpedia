<!DOCTYPE html>
<html>
<head>
  <title>Blocipedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
      <ul class="nav nav-tabs" style="border-bottom:none;">
        <li class="left links"> <%= link_to "Blocipedia", root_path %> </li>
        <li class="left links"> <%= link_to "About", about_path %> </li>
      <span class="right">
        <% if user_signed_in? %>
          Signed in as: <strong><%= current_user.email %></strong> |
          <%= link_to 'Edit Profile', edit_user_registration_path, :class => 'navbar-link' %> or <%= link_to "Sign Out", destroy_user_session_path, :class => 'navbar-link'  %>
        <% else %>
        <span class="sign-up-in-links">
          <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %>
        </span>
        <% end %>
      </span>

      </ul>

      <%= yield %>
  </div>
</body>
</html>
